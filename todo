todo: 

first things:
    unplug 2nd HDD
    install base system
    edit fstab and grub menu 
        -> set to HDD uuids
    
install
    light gui (maybe openbox or xfce)
    subsonic
        --> prolly need some codecs for converting media
        --> check out if subsonic supports PAM 
            this could be handy for user management
            and uploaded files, concatination of music 
    truecrypt
    vim
    ctags
    screen
    urt
    lighttpd
    fwbuilder
    dnsmasq
        --> configure as DHCP and dns server
    set up iptables for nat/pat
    git-core

configuration
    users
        git, urt, personal, some dudes and buddys
    services': see above
    make room in /media for backup and share folder
    webserver for sharing files (/media/share)
    maybe ftp access to those files
    PAM
    set up git reops with git users
    configure gitweb
    and git access to github (seperate repo / folder)
